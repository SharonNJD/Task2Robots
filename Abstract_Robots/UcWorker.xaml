<UserControl x:Class="Robots_inc.UcWorker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Robots_inc"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="200">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="Yellow"/>
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold"/>




        </Style>

    </UserControl.Resources>
    <Border x:Name="BrMain" BorderThickness="1" BorderBrush="Bisque" CornerRadius="15" Margin="10" Background="Aqua">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="{Binding Path=FullName}"/>
            <Label Grid.Row="1" x:Name="lbID" Content="Id:"/>
            <Label Grid.Row="2" x:Name="lbBday" Content="Birth date:"/>
            <Label Grid.Row="3" x:Name="lbRole" Content="Role:"/>

        </Grid>


    </Border>

</UserControl>
